{
  "name": "ema-alert",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm-run-all -p server client",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}